// Create variable to store the city
let city = "Berlin";

// Create variable to store the state
let state = "Berlin";

// Create variable to store the country
let country = "Germany";

alert(`Your Location is : ${country}, ${state}, ${city}`);
